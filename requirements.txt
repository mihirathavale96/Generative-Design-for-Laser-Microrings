# Core ML/Scientific (pin versions for reproducibility)
numpy==2.1.2
scipy==1.16.1
pandas==2.3.1
scikit-learn==1.7.1
scikit-image==0.25.2
networkx==3.3
sympy==1.13.3
mpmath==1.3.0
joblib==1.5.1
threadpoolctl==3.6.0

# Deep Learning (install separately for CUDA/CPU based on environment)
# For Colab or CSF3 GPU: !pip install torch==2.8.0+cu126 torchvision==0.23.0+cu126 triton==3.4.0 --index-url https://download.pytorch.org/whl/cu126
# For CPU only: !pip install torch==2.8.0+cpu torchvision==0.23.0+cpu --index-url https://download.pytorch.org/whl/cpu
# torch==2.8.0+cu126
# torchvision==0.23.0+cu126
# triton==3.4.0

# Image Processing
opencv-python==4.12.0.88
pillow==11.0.0
imageio==2.37.0
tifffile==2025.6.11

# Visualization
matplotlib==3.10.5
seaborn==0.13.2

# Utilities
tqdm==4.67.1
PyYAML==6.0.2
filelock==3.13.1
fsspec==2024.6.1
pytz==2025.2
tzdata==2025.2

# Optional/Deprecated (only if code requires; edit code to use built-in pathlib instead)
#pathlib2==2.3.7.post1

# Notes:
# - Removed Jupyter/notebook internals (pre-installed in Colab; use modules in CSF3 for interactive).
# - Removed NVIDIA CUDA runtimes (use system CUDA via modules in CSF3, e.g., module load cuda/12.6; pre-configured in Colab GPU runtime).
# - Removed sub-deps (auto-resolved by pip).
# - For CSF3: Load appropriate modules (e.g., module load python/3.12 cuda/12.6), create virtualenv, then pip install -r requirements.txt, and install torch separately as commented.
# - For Colab: Enable GPU runtime, pip install -r requirements.txt, then install torch as commented.
